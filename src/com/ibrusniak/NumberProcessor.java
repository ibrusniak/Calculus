package com.ibrusniak;

public class NumberProcessor {
    
    public static NumberContainer addition(NumberContainer operand1, NumberContainer operand2) {
        
        NumberContainer sum = new NumberContainer();
        return sum;
    }
    
    public static NumberContainer substraction(NumberContainer operand1, NumberContainer operand2) {
        
        NumberContainer difference = new NumberContainer();
        return difference;
    }
    
    public static NumberContainer musltiplication(NumberContainer operand1, NumberContainer operand2) {
        
        NumberContainer product = new NumberContainer();
        return product;
    }
    
    public static NumberContainer division(NumberContainer operand1, NumberContainer operand2) {
        
        NumberContainer quotient = new NumberContainer();
        return quotient;
    }
}